# Security

Do not commit real Filen credentials, WebDAV credentials, Docker Hub tokens, GitHub tokens, `.env` files, private keys, or server passwords.

Use `.env.example` only as a template. Copy it to `.env` locally and keep `.env` untracked.

If a credential is exposed, revoke or rotate it immediately.
